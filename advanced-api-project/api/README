# Advanced API Project

## Overview
This project implements a RESTful API for managing books.

## API Endpoints
- `GET /api/books/`: List all books (public).
- `GET /api/books/<id>/`: Get details of a single book (public).
- `POST /api/books